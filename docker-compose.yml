services:
  app:
    image: ghcr.io/sophieneuenmuller/aaee_ra4_proy2_sfr:latest
    container_name: aaee_mapamundi_app_sfr
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: always
